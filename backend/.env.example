# Gemini API Keys (comma-separated, free-tier keys for round-robin rotation)
GEMINI_API_KEYS=your_key_1,your_key_2,your_key_3,your_key_4

# Redis URL (optional, defaults to localhost; uses in-memory fakeredis if unavailable)
# REDIS_URL=redis://localhost:6379/0
